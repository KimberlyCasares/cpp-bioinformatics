# Odd / Even Checker (C++)

## Goal
Determine whether an integer is odd or even using the modulo operator.

## Concepts
- Command-line arguments (`argv`)
- `if/else`
- Modulo operator (`%`)

## Build & Run
```bash
g++ -std=c++17 odd_even.cpp -o odd_even
./odd_even 46753

